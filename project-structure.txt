my-sandbox-sim/
│
├── package.json                # Electron app configuration
├── main.js                     # Electron main process (reads files, handles IPC)
├── preload.js                  # Safe bridge for Context Isolation (CRITICAL)
│
├── src/                        # ENGINE CODE (Logic only, no content)
│   ├── index.html              # Main UI layout
│   ├── engine/                 # GAME LOGIC (pure systems)
│   │   ├── engine.js           # Runtime composition root and boot coordinator
│   │   ├── dataRegistry.js     # Compile + merge system
│   │   ├── dataLoader.js       # IPC wrapper for loading data
│   │   └── systems/            # Focused runtime systems mixed into Engine
│   │       ├── worldSystem.js
│   │       ├── entitySystem.js
│   │       ├── inputSystem.js
│   │       ├── inventorySystem.js
│   │       ├── eventSystem.js
│   │       └── saveSystem.js
│   │
│   ├── config.js               # Global flags
│   └── ui                      # UI elements and components folder
│       ├── components          # Reusable UI pieces
│       ├── renderers           # Focused DOM renderers behind the UI facade
│       └── ui.js               # DOM manipulation and rendering
│
├── data/                       # BASE GAME CONTENT (Data only)
│   ├── stats.json
│   ├── locations.json
│   ├── scenes.json
│   ├── npcs.json
│   └── events.json
│
├── mods/                       # MODDING DIRECTORY
│   └── my_first_mod/      
│       ├── mod.json            # Metadata and load order definition
│       ├── locations.json      # Adds or overwrites locations
│       ├── scenes.json         # Adds or overwrites scripted scene flows
│       └── npcs.json
│
└── saves/                      # MUTABLE STATE
    └── save1.json              # Serialized gameState
